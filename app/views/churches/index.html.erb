<div class="jumbotron">
<h2>LCIYelp! is the best avenue to express how you feel about your home church.</h2>
<h3>So find your location below and write about it. Happy sharing!</h3>
<p><a class="btn btn-info btn-lg" href="#" role="button">Sign up</a></p>
</div>

<table class="table table-hover table-condensed">
  <thead>
    <tr>
      <th>Name</th>
      <th>Address</th>
      <th>Phone</th>
      <th>Resident Pastor</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @churches.each do |church| %>
      <tr>
        <td><%= church.name %></td>
        <td><%= church.address %></td>
        <td><%= church.phone %></td>
        <td><%= church.resident_Pastor %></td>
        <td><%= link_to 'Show', church, class: "btn btn-link" %></td>
        <td><%= link_to 'Edit', edit_church_path(church), class: "btn btn-link" %></td>
        <td><%= link_to 'Destroy', church, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-link" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Church', new_church_path, class: "btn btn-link" %>
